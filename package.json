{
  "name": "wt-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node ./src/index.js",
    "dev": "nodemon --watch src --exec node --experimental-specifier-resolution=node --es-module-specifier-resolution=node ./src/index.js",
    "build": "echo 'build step (if needed)'",
    "lint": "eslint .",
    "test": "echo \"No tests configured yet\" && exit 0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "helmet": "^6.0.1",
    "morgan": "^1.10.0",
    "node-fetch": "^2.6.7"
  },
  "devDependencies": {
    "eslint": "^8.39.0",
    "nodemon": "^3.1.11"
  }
}
